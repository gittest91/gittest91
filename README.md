Hi Kyle, I completed the local application startup and verified the Managed Vault integration successfully. I'm currently trying to complete the end-to-end local testing, but both the Postman request and the PowerShell test are failing at the authentication layer with the following error:

"All token verification methods failed. An invalid DEVELOPMENT TOKEN or an invalid/expired OAuth2 token was provided."

Since the same error occurs in both Postman and PowerShell, could you please let me know if I need a new Development/OAuth2 token or the correct way to generate one for local testing? Once I have a valid token, I'll complete the remaining E2E testing and share the results.
